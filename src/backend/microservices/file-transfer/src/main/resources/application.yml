server:
  port: 8089

spring:
  config:
    import: application-shared.yml

data-repositories:
  upload-isolate-data-repository: S3
  upload-typing-data-repository: S3

logging:
  level:
    root: debug